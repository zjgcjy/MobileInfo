<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ucas.iie.rd6.mobileinfo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.AppInfoActivity">

        </activity>
        <activity
            android:name=".user.UserActivity"
            android:label="User Information">
        </activity>
        <activity
            android:name=".hardware.HardwareActivity"
            android:label="Hardware Information">
        </activity>
        <activity
            android:name=".app.AppActivity"
            android:label="App Information">
        </activity>
        <activity
            android:name=".BasicActivity"
            android:label="Mobile Information">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>